node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env*.local
.vercel
next-env.d.ts
*.tsbuildinfo

# Runtime receipt uploads (local storage stand-in for blob storage)
/public/uploads/*
!/public/uploads/.gitkeep

# Agent skills: the content lives in .agents/skills and is committed.
# .claude/skills only holds junctions/symlinks into it, which git cannot
# represent portably — running `npx skills add` recreates them.
/.claude/skills/
